{
  "main": "src/main.ts",
  "scripts": {
    "build": "ncc build --source-map --minify --license licenses.txt",
    "clean": "rimraf dist lib",
    "all": "npm run clean && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ciemu/action.git"
  },
  "keywords": [
    "actions",
    "node",
    "setup"
  ],
  "dependencies": {
    "@actions/cache": "^3.2.1",
    "@actions/core": "^1.10.0",
    "docker-modem": "^3.0.8",
    "shlex": "^2.1.2"
  },
  "devDependencies": {
    "@types/docker-modem": "^3.0.2",
    "@vercel/ncc": "^0.36.1",
    "rimraf": "^4.4.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
