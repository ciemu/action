{
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc",
    "package": "ncc build --minify --license licenses.txt",
    "all": "npm run build && npm run package"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rodrigo-speller/ciemu.git"
  },
  "keywords": [
    "actions",
    "node",
    "setup"
  ],
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
